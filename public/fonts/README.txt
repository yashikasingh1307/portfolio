Bridge is not bundled here for licensing reasons.

Add Bridge.woff2 (or .otf / .ttf) to this folder (public/fonts/) and it will
be picked up automatically by src/fonts.css. Until then, headings fall back
to Bebas Neue.
